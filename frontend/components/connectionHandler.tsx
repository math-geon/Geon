import React from 'react';
import { IConnectionHandlerProps } from '../interfaces/';

export class ConnectionHandler extends React.Component<IConnectionHandlerProps> {
  constructor(props: IConnectionHandlerProps) {
    super(props);
  }

  render(): JSX.Element {return <></>;}
}
